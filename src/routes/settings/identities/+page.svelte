<svelte:head><title>Identities · Settings</title></svelte:head>
<h2>Identities</h2>
<p class="muted">Addresses you can send as. Managed once compose ships.</p>
<p class="phase">Arrives in <strong>P3</strong>.</p>
<style>
	h2 { font-size: var(--font-size-3); }
	.muted { color: var(--color-text-disabled); }
	.phase {
		margin-top: var(--space-3);
		display: inline-block;
		padding: 4px 10px;
		border: 1px dashed var(--color-border);
		border-radius: var(--radius-md);
		font-size: var(--font-size-0);
		color: var(--color-text-disabled);
	}
</style>
