<svelte:head><title>Rules · Settings</title></svelte:head>
<h2>Rules</h2>
<p class="muted">Deterministic sender rules evaluated before AI at ingest.</p>
<p class="phase">Arrives in <strong>P5</strong>.</p>
<style>
	h2 { font-size: var(--font-size-3); }
	.muted { color: var(--color-text-disabled); }
	.phase {
		margin-top: var(--size-3);
		display: inline-block;
		padding: 4px 10px;
		border: 1px dashed var(--color-outline);
		border-radius: var(--radius-2);
		font-size: var(--font-size-0);
		color: var(--color-text-disabled);
	}
</style>
