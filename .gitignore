node_modules
/.svelte-kit
/build
/dist
.env
.env.*
!.env.example
server/.dev.vars
.dev.vars
secrets.env
TODO.md

# Private deploy configs — the committed wrangler configs carry placeholders, so
# a real hostname / KV namespace id never lands in the public repo.
wrangler.prod.jsonc
server/wrangler.prod.toml
.wrangler
/.turbo
*.log
.DS_Store
/test-results
/playwright-report
